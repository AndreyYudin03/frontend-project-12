{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "online chat",
  "main": "src/index.js",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npx start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci --ignore-scripts"
  },
  "author": "Andrey Yudin",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.3"
  }
}
